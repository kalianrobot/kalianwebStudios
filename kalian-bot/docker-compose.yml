services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  bot:
    build: .
    env_file: .env
    depends_on:
      - redis
    restart: unless-stopped

volumes:
  redis-data:
