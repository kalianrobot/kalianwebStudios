{
  "name": "kalian-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w packages/core",
    "build": "npm run build -w packages/shared && npm run build -w packages/mcp-holded && npm run build -w packages/mcp-icnea && npm run build -w packages/mcp-gmail && npm run build -w packages/mcp-pdf && npm run build -w packages/mcp-booking && npm run build -w packages/mcp-social && npm run build -w packages/core",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
